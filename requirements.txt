# 웹(스트림릿 클라우드)이 설치할 목록.
# ★버전을 딱 하나로 못 박지 않는다. 웹이 쓰는 파이썬 버전에 그 판이 없으면
#   설치가 통째로 실패해 앱이 'Error installing requirements' 로 안 열린다.
#   (PyMuPDF==1.26.7 은 파이썬 3.9 리눅스용이 아예 없다. 그 고정은 내 컴퓨터
#    사정(스마트 앱 제어)이라 웹과는 상관이 없다.)
# anthropic 은 1.x 에서 temperature 를 안 받아 1 미만으로만 묶는다.
streamlit
python-pptx
PyMuPDF
pdfplumber
python-docx
Pillow
anthropic<1
python-dotenv
pdf2image
