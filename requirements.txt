# 웹(스트림릿 클라우드)이 설치할 목록.
# ★버전을 함부로 못 박지 않는다. 웹이 쓰는 파이썬 판에 그 휠이 없으면 설치가
#   통째로 실패해 앱이 'Error installing requirements' 로 안 열린다
#   (PyMuPDF==1.26.7 은 파이썬 3.9 리눅스용이 아예 없다).
# anthropic 은 1.x 에서 temperature 를 안 받아 1 미만으로만 묶는다.
streamlit
python-pptx
# ★PyMuPDF 만은 못 박는다 — 판마다 표를 찾는 결과가 달라서다.
#   안 박아 두면 웹은 최신(1.28.2)을 깔고, 내 컴퓨터는 1.24.14 라서
#   **같은 PDF인데 웹 결과만 다르게** 나왔다(2026-09-17):
#     · 1.28 은 표 테두리를 좁게 잡아 줘, 남은 가장자리 띠를 별개 표로 보고
#       같은 자리에 **텅 빈 유령 표**를 하나 더 만들었다(헌인 3·10·15·34·37쪽).
#     · 1.28 은 천안 7쪽 바깥 표(대주간 의사결정/자금사용 용도/기타사항)를
#       아예 못 찾는다.
#   1.24.14 는 cp39-abi3 휠이라 파이썬 3.9~3.14 리눅스에서 전부 깔린다(확인함).
#   올릴 때는 반드시 검사도구\_전체검사.py 를 그 판으로 먼저 돌려 볼 것.
PyMuPDF==1.24.14
pdfplumber
python-docx
Pillow
anthropic<1
python-dotenv
pdf2image
