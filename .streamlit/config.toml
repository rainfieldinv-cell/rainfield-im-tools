[theme]
# 회사 로고색 남색(네이비)을 포인트 색으로 사용 (계약서 도구·자금판과 통일)
#   이 색이 버튼·선택 표시 등 '강조' 되는 곳에 쓰인다.
#   안 적어 두면 스트림릿 기본색인 빨강이 나온다.
primaryColor = "#1A2B5E"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f6f7f9"
textColor = "#1a1a1a"
font = "sans serif"
